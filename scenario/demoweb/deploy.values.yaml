# demoweb 專案層 Helm 覆蓋
# start_test.sh 會自動載入此檔，並與 k8s/helm/environments/<env>.yaml + run-values 疊加

# 預設 injector 數；若執行時有 -i，會被覆蓋
slaves:
  parallelism: 5

hostAliases:
  - ip: 192.168.29.132
    hostnames:
      - sbdemo.example.com
